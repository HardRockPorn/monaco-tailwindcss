import { State } from './state';
export declare function validateApply(state: State, classNameOrParts: string | string[]): {
    isApplyable: true;
} | {
    isApplyable: false;
    reason: string;
} | null;
