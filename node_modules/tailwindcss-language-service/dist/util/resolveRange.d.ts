import type { Range } from 'vscode-languageserver';
export declare function resolveRange(range: Range, relativeTo?: Range): {
    start: {
        line: number;
        character: number;
    };
    end: {
        line: number;
        character: number;
    };
};
