import { State } from './state';
import type { Root, Rule } from 'postcss';
export declare function bigSign(bigIntValue: any): number;
export declare function generateRules(state: State, classNames: string[]): {
    root: Root;
    rules: Rule[];
};
export declare function stringifyRoot(state: State, root: Root, uri?: string): Promise<string>;
export declare function stringifyRules(state: State, rules: Rule[], tabSize?: number): string;
export declare function stringifyDecls(state: State, rule: Rule, uri?: string): Promise<string>;
export declare function getRuleContext(state: State, rule: Rule, className: string): string[];
