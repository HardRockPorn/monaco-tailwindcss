import type { Range } from 'vscode-languageserver';
export declare function absoluteRange(range: Range, reference?: Range): {
    start: {
        line: number;
        character: number;
    };
    end: {
        line: number;
        character: number;
    };
};
