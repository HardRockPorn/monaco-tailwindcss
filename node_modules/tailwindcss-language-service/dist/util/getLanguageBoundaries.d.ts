import type { TextDocument, Range } from 'vscode-languageserver';
import { State } from './state';
export declare type LanguageBoundary = {
    type: 'html' | 'js' | 'css' | (string & {});
    range: Range;
};
export declare function getLanguageBoundaries(state: State, doc: TextDocument, text?: string): LanguageBoundary[] | null;
