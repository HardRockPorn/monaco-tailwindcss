import { State, Settings } from '../util/state';
import type { TextDocument } from 'vscode-languageserver';
import { InvalidConfigPathDiagnostic } from './types';
export declare function validateConfigPath(state: State, path: string | string[], base?: string[]): {
    isValid: true;
    value: any;
} | {
    isValid: false;
    reason: string;
    suggestions: string[];
};
export declare function getInvalidConfigPathDiagnostics(state: State, document: TextDocument, settings: Settings): InvalidConfigPathDiagnostic[];
